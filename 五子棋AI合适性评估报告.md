# 弈境五子棋 AI 合适性评估报告

**审查范围：** 当前 Android 项目的 `GomokuAi.kt` 与 `AiDifficulty` 配置；网络资料检索截至 2026 年 8 月 28 日。  
**适用规则：** 15×15 自由五子棋（连成五子或以上即胜）。

## 结论摘要

当前 AI **不是“最强大”的五子棋 AI，也不应以“大师/最强”作为棋力承诺**；它尚未达到竞赛引擎或高水平连珠程序的技术层级。经典研究以**威胁空间搜索**与**证明数搜索**解决特定五子棋变体，现代强引擎则结合专项威胁求解、精细模式评估、深度 Alpha-Beta 或神经网络评估。[1] [2] [3]

但是，对于首版、离线、原生 Android、面向普通用户的人机对战，本方案的主路线仍是**合适的基础选择**：它没有网络依赖，使用时间预算限制响应时间，具备一手胜负防守、局部候选、迭代加深 Alpha-Beta 和 Zobrist 哈希等必要骨架。正确的产品定位应是“**轻量级离线战术 AI**”或“**强实用 AI**”，而不是“最强 AI”。

> **建议：保留现有架构作为新手/普通档；在发布可宣称为强力或大师级的难度之前，优先完成模式识别、完整威胁搜索与正确的置换表边界处理。** 这比直接接入泛用 MCTS 或大模型更符合移动端成本、延迟和可维护性目标。

| 判断维度 | 当前结论 | 说明 |
|---|---|---|
| 普通用户的离线体验 | **合适** | 无须联网，难度预算为 60–1,500 ms，且 AI 在后台线程计算。 |
| 初学者/休闲玩家棋力 | **足够** | 能处理立即成五、立即堵四，并能发现部分双威胁。 |
| 强战术与稳定性 | **不足** | 不是完整 TSS/VCF/VCT；复杂强迫序列与跳型威胁覆盖不够。 |
| “大师/竞赛级”定位 | **不合适** | 缺少专项求解器、精细棋型体系、选择性搜索与实战基准。 |
| 端侧性能方向 | **方向正确但未优化充分** | Alpha-Beta 很适合端侧；现有 Kotlin 评估/候选生成仍会重复全盘扫描。 |
| 最强方案 | **不应直接更换为 MCTS** | 若目标是高棋力，应采用五子棋专项的 TSS/VCF + 改良 Alpha-Beta；更长期才考虑自训 NNUE/轻量网络。 |

## 现有实现审查

当前 `GomokuAi` 的执行次序为：生成距已有棋子两格内的候选点；检查己方一手胜、对方一手胜并封堵；检查部分“双威胁”；随后按难度在候选点上执行迭代加深 Alpha-Beta。评估器为四方向的连续子数与开口数打分，并使用棋盘的 Zobrist 哈希作为置换表键。

| 组件 | 当前实现 | 审查意见 |
|---|---|---|
| 即时战术 | 己方一手成五、对手一手成五、候选前 16 点的双威胁 | **必要且有效**，能显著改善普通对局。 |
| 候选生成 | 已落子周围两格；按攻防分数排序；每层固定保留 6–14 个 | 移动端友好；但固定截断和两格范围会漏掉部分低分但关键的远端防御/构形点。 |
| 搜索 | 迭代加深、Alpha-Beta、1–5 层、60–1,500 ms | 路线正确；深度和分支规模不足以稳定覆盖多层强迫手。 |
| 评估 | 连续石 + 开口数的加权和 | 过粗；不能系统区分跳三、跳四、眠三、冲四、双三、双四等模式。 |
| 缓存 | 每次 `chooseMove` 清空的 HashMap，存深度/分数/最佳点 | 有基本收益；但无 `EXACT/LOWER/UPPER` 边界类型，Alpha-Beta 截断结果被当作精确值复用，可能影响正确性和排序。 |
| 难度 | 时间、深度、候选宽度；新手从前三手随机 | 适合产品化；低档仍坚持必胜必防，体验不会出现明显“送五”。 |
| 规则适配 | 仅自由规则 | 与当前产品一致；若加入连珠禁手，需重写合法着生成和威胁判定。 |

### 已做对的部分

首先，当前设计在每个 AI 回合使用有限时间预算，而不是无界搜索；这对于手机端防止长时间占用 CPU 很重要。其次，Alpha-Beta 配合候选排序和迭代加深，是完全信息棋类在受限算力下的可靠工程基础。开源项目 GomokuZero 也将带 Alpha-Beta 的 Minimax、Zobrist、候选排序缓存、威胁空间搜索与主变化搜索组合使用，说明当前路线的基础方向没有偏离五子棋工程实践。[2]

其次，预先检查“一手胜/一手必防”将最常见且最容易被用户察觉的漏防问题移出普通搜索树。这对新手到中级玩家尤其重要。当前候选局部化也将 15×15 棋盘上的 225 个合法点收缩为有限威胁邻域，是响应时间可控的前提。

### 不能称为“最强”的关键原因

**第一，当前双威胁检测不等于完整威胁空间搜索。** 它只尝试某一个候选后是否出现两个下一手成五点；它不递归构建“进攻方连续制造威胁—防守方仅能在有限应手中选择”的树，因此无法可靠求解 VCF/VCT 等多层强迫序列。Allis 等人的 Victoria 之所以能在两个常见变体中建立博弈论结果，关键是专门的威胁空间搜索与证明数搜索，而不是普通的浅层评估搜索。[1]

**第二，棋型表达不足。** 现有分数只观察落点两侧的连续同色子和开口数。它没有成为一等公民的跳四、跳三、冲四、活三、双四、双三，也不能将棋型之间的优先级和规则合法性统一编码。高质量五子棋 AI 的评估器通常使用方向线序列或模式表；GomokuZero 的公开说明也使用线性序列和威胁类型缓存，而不是仅用连续长度。[2]

**第三，置换表复用不够严谨。** 在 Alpha-Beta 中，剪枝节点的值通常只是上界或下界。当前表只存 `depth/score/bestIndex`，下次命中便直接返回该分数，未标记 `EXACT`、`LOWER_BOUND` 或 `UPPER_BOUND`。这不是内存/速度问题，而是走法质量的正确性问题；应先修复。

**第四，缺少棋力基准。** 当前项目有 6 项规则、即时战术、触控和先手偏好测试，但没有对公开残局集、VCF/VCT 集或固定对手进行大量对局测试。没有“战术题成功率、固定预算下的节点数、对基准引擎胜率或 Elo”的实测数据，不能客观证实“大师”强度。

## 外部技术路线比较

| 路线 | 棋力上限 | Android 离线适配 | 训练/集成成本 | 法律与发布边界 | 建议 |
|---|---:|---:|---:|---|---|
| **当前：浅层 Alpha-Beta + 简化棋型** | 中等 | 很好 | 低 | 自主代码 | 保留为新手/普通档基础。 |
| **强化：模式表 + TSS/VCF + PVS/正确 TT** | 高 | 很好 | 中等 | 可自主实现 | **首选下一步**。 |
| **Rapfi JNI 引擎** | 很高 | 可行，但需 NDK/ARM NEON 适配 | 高 | Rapfi 为 GPL-3.0；分发需满足 GPL 源码义务 | 仅在接受 GPL 发布策略时考虑。 |
| **轻量自训网络 + Alpha-Beta/MCTS** | 高至很高 | 可行，需模型与真机压测 | 很高 | 自建训练数据/模型许可 | 中长期“旗舰 AI”路线。 |
| **通用大语言模型远程落子** | 不稳定 | 差，需要网络 | 高且持续付费 | 隐私、时延、不可重复 | 不适合规则性五子棋引擎。 |

### 为什么不建议直接换成 MCTS

MCTS 与神经网络在强棋类中有效，但五子棋的强制威胁非常尖锐。专项的 Alpha-Beta、TSS/VCF 往往能更直接地证明进攻或防守序列。2025 年的 Rapfi 研究表明，在无 GPU 的受限算力条件下，基于模式码表、增量更新和深度优先 Alpha-Beta 的专用评估可达到很高效率；其论文报告在受限计算环境中超过基于 CNN 的代理，并在 2024 GomoCup 获冠。[3] 因此，下一步应加强**特化评估 + 强迫搜索**，而不是仓促用泛用 MCTS 替换当前算法。

### “最强”引擎的现实边界

Gomocup 的公开下载页中，2026 年榜单将 RAPFI、KATAGOMO、ALPHAGOMOKU(MK) 等列在前列；这些竞赛引擎的长期积累包括 NNUE/神经网络、专项 VCF、自动调参、针对规则集的模型与高性能原生实现。[5] 这证明当前 Kotlin AI 不属于最强行列。

Rapfi 公开仓库称其为 Gomoku/Renju 引擎，使用先进 Alpha-Beta、经典评估与 NNUE，且可面向 ARM64/NEON 编译；但项目许可证为 GPL-3.0，并要求分发时提供相应源码与许可证信息。[4] 因此，**不能将其代码直接复制进当前仓库**，除非整个分发模式和许可策略接受 GPL 的要求。把 Rapfi 当作强度标杆、测试对手或研究参考是可行的；直接集成是另一项产品/法律决策。

## 推荐的工程路线

### 近期：将“强实用 AI”做扎实

建议将当前的“新手/普通/困难/大师”暂时改为“新手/普通/困难/挑战”，或保留“大师”仅作为 UI 名称但不要对外承诺竞技级强度。优先完成如下四项，工作量相对可控，收益最大。

| 优先级 | 改动 | 目的 | 预期收益 |
|---|---|---|---|
| P0 | 置换表加入 `EXACT/LOWER/UPPER`、最大容量与替换策略 | 修正剪枝缓存的语义 | 提升稳定性与搜索可信度。 |
| P0 | 完整 9–11 格方向模式表 | 精确识别活四、冲四、跳四、活三、眠三等 | 显著减少漏攻漏防。 |
| P1 | 实现递归 TSS/VCF 求解器，仅用于强制威胁局面 | 捕获多手杀棋和必防序列 | 强档棋力提升最大。 |
| P1 | PVS/Negascout、killer/history 启发、威胁延伸或静态搜索 | 让高价值着优先被搜索 | 在相同 700–1,500 ms 内搜索更深。 |
| P2 | 增量更新候选分数、模式缓存和固定容量数组/位集合 | 消除每节点反复扫描全盘 | 降低 GC 与功耗，改善中低端机流畅性。 |

实现 P0–P1 后，再以真机 700 ms 和 1,500 ms 预算测量以下指标：战术残局正确率、p50/p95 落子耗时、搜索节点数、内存峰值、连续 20 局温升/耗电，以及对固定基准版本的胜率。只有在这些指标稳定后，才应上调最高难度的对外表述。

### 中期：提供真正的“旗舰 AI”

若产品目标是尽可能高的本地棋力，建议建立**独立的原生引擎模块**：Kotlin/Compose 只负责 UI 和对局状态，NDK C++ 模块负责模式表、TSS/VCF、Alpha-Beta 与增量评估。自研实现可避免 GPL 继承风险，但需投入严格的单元测试和交叉验证。

之后可以评估自训轻量网络或 NNUE。LiteRT 官方说明，训练后量化能减小模型并改善推理延迟；其文档列出动态范围量化约 4 倍缩小、CPU 约 2–3 倍加速，整型量化约 4 倍缩小、CPU 3 倍以上加速。完整整型量化需要代表性数据校准，并必须验证量化后的准确度。[6] 这说明模型端侧化技术可行，但训练数据、策略/价值校准和 Android 真机回归测试仍是主要成本，不能替代专项战术求解。

## 最终建议

**当前 AI 是适合首版离线 Android 应用的“正确基础”，但不是最合适的终局方案，也绝非最强方案。** 如果目标是“多数用户觉得强、响应快、包体轻、无需联网”，保留并强化该路线是最优选择。如果目标是“挑战高水平五子棋玩家或对标竞赛引擎”，则应采用“**精细棋型表 + 完整 TSS/VCF + 改进 Alpha-Beta**”的专项引擎路线；神经网络/NNUE 作为后续增强，而非下一次迭代的首要工作。

## 参考资料

[1] [Allis, van den Herik, Huntjens, *Go-Moku Solved by New Search Techniques*, AAAI](https://aaai.org/papers/0001-fs93-02-001-go-moku-solved-by-new-search-techniques/)

[2] [JiachenRen, *GomokuZero* 开源项目](https://github.com/JiachenRen/GomokuZero)

[3] [Jin et al., *Rapfi: Distilling Efficient Neural Network for the Game of Gomoku*, arXiv, 2025](https://arxiv.org/html/2503.13178)

[4] [dhbloo, *Rapfi* 开源引擎与 GPL-3.0 许可说明](https://github.com/dhbloo/rapfi)

[5] [Gomocup, *Download Gomoku Artificial Intelligence* — 2026 排名与引擎说明](https://gomocup.org/download-gomoku-ai/)

[6] [Google AI Edge, *Post-training quantization*](https://ai.google.dev/edge/litert/models/post_training_quantization)
